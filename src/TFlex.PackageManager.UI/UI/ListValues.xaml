<Window x:Class="TFlex.PackageManager.UI.ListValues"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="300" 
        Width="300" 
        WindowStyle="ToolWindow" 
        ResizeMode="NoResize">

    <DockPanel TextOptions.TextFormattingMode="Display" UseLayoutRounding="True">

        <StackPanel DockPanel.Dock="Bottom" 
                    Margin="10" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Height="24">
            <Button Name="button1" 
                    Width="80" 
                    Padding="2" 
                    Click="Button1_Click">OK</Button>
            <Button Name="button2" 
                    Width="80" 
                    Padding="2" 
                    Margin="10,0,0,0" 
                    Click="Button2_Click">Cancel</Button>
        </StackPanel>

        <TextBox Name="textbox" TextWrapping="Wrap" AcceptsReturn="True" Margin="10" />

    </DockPanel>
</Window>
